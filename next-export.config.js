// /** @type {import('next').NextConfig} */
// module.exports = {
// 	exportPathMap: async function (defaultPathMap, _ctx) {
// 		process.env.ANALYZE === 'true' && console.log({ defaultPathMap, _ctx })
// 		/** @type {import('next/dist/server/config-shared').ExportPathMap } webConfig */
// 		return defaultPathMap
// 	},
// }
